using UnityEngine;
using System.Collections;

public class EmotionObject : MonoBehaviour {

    public float appeal = 0.0f;
    // prais to hit it
    public float prais = 0.0f;
    // desirability to hit it
    public float desirability = 0.0f;
    public float relationship = 0.0f;
    
	// Use this for initialization
	void Start () {
                
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
